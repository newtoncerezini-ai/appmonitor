{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn monitoramento.wsgi",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 10
  }
}
